
{
  "name": "ak",
   "homepage": "https://guillote87.github.io/ak/",
  "version": "1.0.0",
  "description": "AK PRODUCTOS VETERINARIOS",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "bugs": {},
  "dependencies": {
    "curl": "^0.1.4",
    "gh-pages": "^3.2.3",
    "node": "^17.7.2",
    "react-script": "^2.0.5",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "concurrently": "^7.1.0"
  }
}
